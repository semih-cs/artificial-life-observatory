{
  "name": "artificial-life-observatory",
  "private": true,
  "version": "0.1.0",
  "description": "Artificial Life Observatory — simulation core (0A), experiment harness (0B), persistence and world runner (0C), Observatory frontend (0D).",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "build": "npm run build -w packages/simulation-core && npm run build -w packages/experiment-harness -w packages/persistence && npm run build -w packages/world-runner && npm run build -w packages/observatory",
    "simulate": "npm run simulate --workspace=packages/simulation-core --",
    "experiment": "npm run experiment --workspace=packages/experiment-harness --",
    "world": "npm run world --workspace=packages/world-runner --",
    "observatory": "npm run dev --workspace=packages/observatory --"
  }
}
